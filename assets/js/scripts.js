/*
 * Dont forget to uncomment .pipe(uglify()) in gulpfile.js
 * ([scripts] task) before deploying for production as it
 * is quite slow for developement.
 * Also, comment this console.log out..
 */
console.log("%c => Don't forget to uncomment .pipe(uglify()) in gulpfile.js ([scripts] task) before deploying for production ", "background: #666; color: #f1f1f1; display: block;")

/*
 * Custom scripts
 */
$(function () {
    let one = 1;
    console.log(one);
});